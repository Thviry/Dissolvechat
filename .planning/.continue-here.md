---
phase: ui-polish
task: task-8-review-fixes
status: in_progress
last_updated: 2026-02-28T10:01:26.841Z
worktree: .claude/worktrees/admiring-almeida
branch: claude/admiring-almeida
---

<current_state>
Doing Task 8 final review pass on the UI polish branch. User walked through checklist and
found 3 issues. We are mid-fix — two fixes remain unfinished.
</current_state>

<completed_work>

- Task 1: Icons.jsx SVG library — Done (committed)
- Task 2: App.css rebuild — Done (committed)
- Task 3: LoginScreen.jsx redesign — Done (committed in 09079b5)
- Task 4: Sidebar.jsx redesign + settings overlay — Done (committed in 09079b5)
- Task 5: ChatPanel.jsx redesign — Done (committed in 09079b5)
- Task 6: ShareModal + PassphraseModal SVG close buttons — Done (committed in 09079b5)
- Task 7: App.jsx session restore + Toast/PassphraseModal system — Done (committed in 09079b5)
- Desktop port: All client changes mirrored to desktop/src/ — Done (committed in 2fc56a6)
- Seed phrase files copied from 91ef5c3: seed.js, MnemonicScreen.jsx, OnboardingScreen.jsx,
  useIdentity.js — copied to client/src/ but NOT yet committed

</completed_work>

<remaining_work>

**Fix 1 — Seed phrase (PARTIALLY DONE, NOT COMMITTED)**
Files already copied from commit 91ef5c3 to client/src/:
- `client/src/crypto/seed.js` ✓ copied
- `client/src/components/MnemonicScreen.jsx` ✓ copied
- `client/src/components/OnboardingScreen.jsx` ✓ copied
- `client/src/hooks/useIdentity.js` ✓ copied

Still needed:
1. Add ghost button + mnemonic CSS to `client/src/App.css`
   — The CSS block is in /tmp/App_seed_tail.css (search for "btn-ghost" and "mnemonic-grid" in
     commit 91ef5c3's App.css diff — or grep `git show 91ef5c3:client/src/App.css | tail -100`)
2. Update `client/src/components/LoginScreen.jsx`:
   — Change `showEnroll` boolean to `view` state ("home" | "enroll" | "recover")
   — Add "Recover from Seed Phrase" ghost button on home view
   — Add the recover view (mnemonic textarea + word count validation + BIP39 checksum)
   — Import `validateMnemonic` from `../crypto/seed`
   — Add `onRecover` prop
   The full diff is in: `git show 91ef5c3 -- client/src/components/LoginScreen.jsx`
3. Update `client/src/App.jsx`:
   — Add `pendingMnemonic` state, `mode` gets "mnemonic" and "onboarding" values
   — Add `handleRecover` callback using `identity.recover()`
   — Import MnemonicScreen, OnboardingScreen
   — handleEnroll now expects `{ mnemonic }` from `identity.enroll()` and sets mode="mnemonic"
   — Add LoginScreen `onRecover={handleRecover}` prop
   — Add mnemonic and onboarding render blocks
   The full pattern is in: `git show 91ef5c3:client/src/App.jsx` (lines 20-310)
   IMPORTANT: Use our Toast/PassphraseModal version as base — 91ef5c3 still uses alert()/prompt()
4. Mirror all seed phrase changes to `desktop/src/` as well
5. Commit everything

**Fix 2 — Bubble animation (NOT STARTED)**
Problem: `animation: msg-in 0.15s ease` fires on ALL `.chat-bubble` elements on mount
(all animate simultaneously), not just new messages.
Fix: Track seen message IDs and only apply animation class to truly new messages.
Approach:
- Add `const seenRef = useRef(null)` in ChatPanel
- On render, if seenRef.current is null OR peer changed: reset to current message IDs
- After render (useEffect no-deps): add all current msgIds to seenRef
- In bubble className: add `is-new` class if msgId not in seenRef.current
- In App.css: move `animation: msg-in` from `.chat-bubble` to `.chat-bubble.is-new`
File: `client/src/components/ChatPanel.jsx`

**Fix 3 — Handle checking (DIAGNOSED, NO CODE CHANGE NEEDED)**
The handle checking code is correct. The relay server must be running on localhost:3001
for it to show ✓/✗. Without relay: shows "Could not check — is the relay running?" error.
Tell user: start relay with `cd server && npm start` before testing enrollment.

**Task 8 checklist items still unverified** (run `npm run dev` in admiring-almeida/client):
- Login screen wordmark, tagline, buttons
- Create Identity flow + seed phrase display after enroll
- Recover from Seed Phrase option
- Sidebar SVG icons, settings overlay
- Chat bubble animation (after Fix 2)
- All 5 themes
- Session restore spinner

</remaining_work>

<decisions_made>

- Porting seed phrase from commit 91ef5c3 (separate branch, never merged into ui-polish branch)
- Using 91ef5c3's useIdentity.js directly (enroll now returns { mnemonic }, adds recover())
- App.jsx mnemonic mode: keep OUR Toast/PassphraseModal base, add mnemonic/onboarding
  modes and handleRecover on top — do NOT use 91ef5c3's alert()/prompt() version
- Bubble animation fix: track seenRef by peer to avoid false animations on contact switch
- Desktop port: everything applied to client/ must also be mirrored to desktop/src/

</decisions_made>

<blockers>

- Handle checking requires relay running: `cd server && npm start` (or `node server/index.js`)
- Context window ran out mid-fix — resume to finish

</blockers>

<context>
This is the UI polish branch (admiring-almeida worktree). The plan is at
docs/plans/2026-02-27-ui-polish-implementation.md. All 8 tasks are conceptually done
but Task 8 review found 3 issues. The seed phrase work is the most critical — it's a
missing feature, not just a visual bug. The 91ef5c3 commit has all the seed phrase
logic. When merging into App.jsx, use git show to read the 91ef5c3 version as reference
but build on top of our polished App.jsx (which has Toast, PassphraseModal, useToast).
</context>

<next_action>
Start with: Add ghost button + mnemonic CSS to client/src/App.css
  `git show 91ef5c3:client/src/App.css | grep -A 60 "btn-ghost"`
Then: Update LoginScreen.jsx to add recover view
  `git show 91ef5c3 -- client/src/components/LoginScreen.jsx`
Then: Update App.jsx to add mnemonic/onboarding modes
  `git show 91ef5c3:client/src/App.jsx`
Then: Mirror to desktop/src/
Then: Fix ChatPanel.jsx bubble animation
Then: Commit and complete Task 8 checklist
</next_action>
